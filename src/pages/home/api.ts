import { SomeObject } from '../../common/types';
// TODO: тут надо настроить вебсокеты
/** Отправить сообщение */
const sendMessage = (props: SomeObject) => {
  console.log('sendMessage', props);
};

export default sendMessage;
